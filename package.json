{
  "name": "sticky-notes",
  "version": "2.1.5",
  "description": "Sticky Notes Chrome Extension",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "jest",
    "lint": "eslint modules/ background.js content.js --rule 'no-undef: off' --rule 'no-unused-vars: off'"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.56.0",
    "linkedom": "^0.18.0"
  },
  "jest": {
    "testMatch": ["**/tests/**/*.test.js"],
    "environment": "node"
  }
}